#compare all parameters with first parameter(if second parameter equals first we print third)
#for oracle
SELECT DECODE(3 * 4 , 12, 'twelve') ;

SELECT IF(3 * 4, 12, 'twelve');
SELECT IF(3 * 4, 13, 'twelve');


